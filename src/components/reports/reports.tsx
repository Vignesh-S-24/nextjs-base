'use client'

import { Box } from "@mui/material";
import React from "react";

const Reports = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Reports
    </Box>
  );
};

export default Reports;
