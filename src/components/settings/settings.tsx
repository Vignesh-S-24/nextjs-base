'use client'

import { Box } from "@mui/material";
import React from "react";

const Settings = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Settings
    </Box>
  );
};

export default Settings;
