'use client'

import { Box } from '@mui/material'
import React from 'react'

const Profile = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Profile
    </Box>
  )
}

export default Profile