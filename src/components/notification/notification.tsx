'use client'

import { Box } from "@mui/material";
import React from "react";

const Notification = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Notification
    </Box>
  );
};

export default Notification;
